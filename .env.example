# ============================================================================
# SmartSupp - Variáveis de Ambiente
# ============================================================================
# 
# Copie este arquivo para .env e preencha com seus valores:
#   cp .env.example .env
#
# IMPORTANTE: Nunca commite o arquivo .env no Git!
# ============================================================================

# ----------------------------------------------------------------------------
# APP CONFIGURATION
# ----------------------------------------------------------------------------
APP_NAME=SmartSupp
DEBUG=True
TESTING=False
VERSION=0.1.0

# ----------------------------------------------------------------------------
# DATABASE (PostgreSQL)
# ----------------------------------------------------------------------------
# Configurações do banco de dados PostgreSQL
# Certifique-se de que o PostgreSQL está rodando via docker-compose
# ou configure com suas credenciais locais
POSTGRES_USER=smartsupp
POSTGRES_PASSWORD=smartsupp_dev
POSTGRES_DB=smartsupp
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ----------------------------------------------------------------------------
# SECURITY
# ----------------------------------------------------------------------------
# Gere uma chave secreta forte para JWT tokens
# Para gerar uma chave segura, execute:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
#   ou
#   openssl rand -hex 32
SECRET_KEY=change-me-in-production-use-openssl-rand-hex-32-or-secrets-token-urlsafe
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ----------------------------------------------------------------------------
# LLM - Google Gemini 2.5 Flash
# ----------------------------------------------------------------------------
# Escolha UMA das opções abaixo:

# OPÇÃO 1: Google AI Studio (Recomendado para desenvolvimento local)
# Obtenha sua API Key em: https://makersuite.google.com/app/apikey
# É mais simples para desenvolvimento e testes
GOOGLE_API_KEY=your-google-api-key-here

# OPÇÃO 2: Vertex AI (Para produção ou se preferir)
# Descomente e configure se preferir usar Vertex AI ao invés de Google AI Studio
# Requer Google Cloud Project e service account
# GOOGLE_CLOUD_PROJECT=your-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# VERTEX_AI_LOCATION=us-central1

# Modelo do Gemini (padrão funciona para ambas as opções)
GEMINI_MODEL=gemini-2.0-flash-exp

# ----------------------------------------------------------------------------
# NOTAS IMPORTANTES
# ----------------------------------------------------------------------------
#
# 1. DATABASE:
#    - Se usar docker-compose, as credenciais acima devem corresponder
#      às configurações no docker-compose.yml
#    - Para rodar migrations: alembic upgrade head
#    - Para popular dados: poetry run seed-all
#
# 2. SECRET_KEY:
#    - GERE UMA CHAVE ÚNICA e SEGURA para produção!
#    - A chave de exemplo acima NÃO deve ser usada em produção
#
# 3. LLM (Google Gemini):
#    - OPÇÃO 1 (Google AI Studio) é mais fácil para desenvolvimento
#    - OPÇÃO 2 (Vertex AI) é melhor para produção
#    - Se ambas estiverem configuradas, Vertex AI tem prioridade
#    - Para desenvolvimento local, recomenda-se usar GOOGLE_API_KEY
#
# 4. TESTING:
#    - Para rodar testes, pode usar o mesmo banco ou criar um banco
#      separado (smartsupp_test)
#    - Configure TEST_DATABASE_URL se necessário
#
# ============================================================================
